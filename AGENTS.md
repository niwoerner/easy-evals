- For real agent E2E tests, use latest Pi through OpenRouter: `pi --provider openrouter --model openai/gpt-5.6-luna --thinking low|medium`. `OPENROUTER_API_KEY` is configured; install/update Pi with `npm i -g @earendil-works/pi-coding-agent@latest`.

- Add no types/functions beyond those requested; avoid type indirection and tiny helpers. Get approval before adding or changing API types.
